export const successTip = title => {
    wx.showModal({
        title: title,
        showCancel: false
    });
}
export const failTip = title => {
    wx.showModal({
        title: title,
        showCancel: false
    });
}